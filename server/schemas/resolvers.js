const { User, Book } = require('../models')

const resolvers = {
    Query: {
//get a single user by either id or username
    },

    Mutation: {

//login a single user
// create a user
// save a book
// delete a book 
    }
}